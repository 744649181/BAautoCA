# ⚡ 挂机电力平衡分流器设计器 (Power Balance Splitter Designer)

本工具用于计算如何利用分流器（Splitter）和不同种类的电池组合，达成特定的输出功率需求。

## ✨ 主要功能

1.  **精确计算**：输入目标功率，自动计算所需的分流网络结构。
2.  **多电池支持**：支持自定义多种电池/发电设备（如不同种类的电池），算法会自动寻找最佳组合。
3.  **多方案对比**：
    *   **🎯 精准优先**：尽可能接近目标功率，溢出最小。
    *   **🧩 结构精简**：优先考虑更简单的网络结构（更少的设备、更浅的分流层数）。
    *   **⚖️ 均衡方案**：在复杂度和精确度之间寻找平衡。
4.  **可视化展示**：自动生成分流器连接图，直观展示每个分流器的出口设置。
5.  **自动存档**：自定义的电池数据会自动保存在浏览器中，无需重复输入。

## 📖 使用教程

### 1. 🔋 电池库管理 (Battery Library)
在页面顶部的 **"电池库管理"** 区域：
*   **添加电池**：输入名称、满载功率 (P) 和 消耗速率 (C)。
    *   *满载功率 (P)*：单个设备最大的输出功率。
    *   *持续时间 (T)*：单个燃料物品能让设备满功率运行的时间（秒）。例如：一个煤炭能烧80秒，则填 `80`。
*   **启用/禁用**：勾选列表中的 ✅，只有被勾选的电池才会参与计算。
*   **删除**：点击 🗑️ 图标删除不需要的电池预设。

### 2. 📊 参数设置
在 **"输入参数"** 区域填写系统参数：
*   **目标功率 (D)**：你希望系统最终输出的总功率。
*   **基地发电 (P_base)**：自身已有的基础发电量（不经过分流器的恒定输出）。
*   **燃料供应速率 (R)**：主传输带上燃料（如煤炭、电池物品）的供应速度（个/秒）。
    *   *例如*：如果每2秒产生1个燃料，则填 `0.5`。（主传输带参数，如后续基地参数改变，可以修改这项值）
*   **搜索深度**：分流器网络的最大层数。默认为 5，层数越高计算越慢但精度越高（最大推荐 8）。

### 3. ⚙️ 生成方案
点击 **"计算最优方案"** 按钮。系统将瞬间为您生成结果。

### 4. 📈 查看结果
在结果区域，您可以：
*   **切换方案**：点击顶部的标签卡（如 "精准优先"、"结构精简"）查看不同策略的方案。
*   **查看详情**：
    *   **超出功率**：显示当前方案比目标功率多出多少（越小越好）。
    *   **电池数量/利用率**：查看需要多少个电池以及燃料的利用效率。
*   **网络结构图**：
    *   中间的 **Canvas 绘图** 直观展示了燃料的流向。
    *   下方文字区域列出了详细的连接层级（例如：`[三出口分流器] -> 出口1 -> 电池A`）。

## 🧮 算法原理

分流器（Splitter）通常可以将物品流均分为 2 份或 3 份。本工具利用数学算法，将目标比例拆解为 `(1/2)^a × (1/3)^b` 的组合形式。

通过动态控制燃料进入电池的概率（占空比），可以控制电池的平均输出功率：
> 输出功率 = 电池满载功率 × (燃料输入速率 / 电池消耗速率)

## 🛠️ 技术栈
*   HTML5 / CSS3 (Glassmorphism UI)
*   JavaScript (ES6+)
*   No external frameworks (Pure Vanilla JS)

